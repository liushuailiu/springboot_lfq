<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.lfq.dao.AdminDao">
    <!--登录-->
    <select id="LoginAdmin" resultType="com.baizhi.lfq.entity.Admin">
        select id,name,password,level from admin where name=#{name} and password=#{password}
    </select>
</mapper>
